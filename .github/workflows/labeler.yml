name: "Pull Request Labeler"
on:
- pull_request

jobs:
  labeler:
    runs-on: ubuntu-latest
    steps:
    - id: label-the-PR
      uses: actions/labeler@v5
      with: 
        sync-labels: true
  
